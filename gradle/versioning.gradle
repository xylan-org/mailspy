
ext.updateBuildVersion = { newVersion ->
    def buildFileContents = buildFile.getText()
        .replaceFirst("version = \".*\"", "version = \"" + newVersion + "\"")
    buildFile.setText(buildFileContents)
}
